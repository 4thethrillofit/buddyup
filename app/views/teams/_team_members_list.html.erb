<ul>
  <% team.members.each do |member| %>
    <li><%= member.name %>, <%= member.email %> <%= link_to "Remove Member", team_team_assignment_path(team.object, member.object), method: "DELETE", class: "btn btn-danger" %></li>
  <% end %>
</ul>